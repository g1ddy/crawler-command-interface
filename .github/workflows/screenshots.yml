name: Update Documentation Screenshots

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]
    paths:
      - 'src/**'
      - 'app/**'
      - 'data/**'
      - 'public/**'
      - 'index.html'
      - 'vite.pages.config.ts'
      - 'playwright.screenshots.config.ts'
      - 'tests/screenshots/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/screenshots.yml'

permissions:
  contents: write

jobs:
  screenshots:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout pull request branch
        uses: actions/checkout@v4
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          ref: ${{ github.event.pull_request.head.ref }}

      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm

      - run: npm ci

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Generate documentation screenshots
        run: npm run test:screenshots
        env:
          CI: true

      - name: Verify canonical screenshot set
        shell: bash
        run: |
          set -euo pipefail
          expected=(
            screenshot-crawler.png
            screenshot-inventory.png
            screenshot-skills.png
            screenshot-journal.png
            screenshot-crawler-overview.png
            screenshot-crawler-achievements.png
            screenshot-crawler-broadcast.png
          )
          for filename in "${expected[@]}"; do
            test -s "docs/images/${filename}"
          done

      - name: Upload generated screenshots
        uses: actions/upload-artifact@v4
        with:
          name: crawler-documentation-screenshots-${{ github.event.pull_request.number }}
          path: docs/images/*.png
          if-no-files-found: error
          retention-days: 14

      - name: Commit refreshed documentation screenshots
        if: ${{ github.event.pull_request.head.repo.full_name == github.repository }}
        shell: bash
        env:
          HEAD_BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            docs/images/screenshot-crawler.png \
            docs/images/screenshot-inventory.png \
            docs/images/screenshot-skills.png \
            docs/images/screenshot-journal.png \
            docs/images/screenshot-crawler-overview.png \
            docs/images/screenshot-crawler-achievements.png \
            docs/images/screenshot-crawler-broadcast.png
          if git diff --cached --quiet; then
            echo "Documentation screenshots are already current."
            exit 0
          fi
          git commit -m "docs(assets): refresh interface screenshots"
          git push origin "HEAD:${HEAD_BRANCH}"
