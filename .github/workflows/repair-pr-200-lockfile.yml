name: Repair PR 200 lockfile

on:
  push:
    branches:
      - migrate-ratings-notifications-modular-hud-4299192838521679423

permissions:
  contents: write

jobs:
  repair:
    if: github.ref_name == 'migrate-ratings-notifications-modular-hud-4299192838521679423'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm
      - run: npm install --package-lock-only --ignore-scripts --no-audit --no-fund
      - name: Commit regenerated lockfile
        run: |
          if git diff --quiet -- package-lock.json; then
            echo "package-lock.json already matches package.json"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package-lock.json
          git commit -m "fix: regenerate package lockfile"
          git push
