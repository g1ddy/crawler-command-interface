"use client";
import type { StatBreakdown } from "../../../../app/domain/stats";

export function StatInspectorModal({ breakdown, onClose }: { breakdown: StatBreakdown; onClose: () => void }) {
  return <div className="modal-backdrop" onClick={onClose}><div className="modal-content panel" onClick={(event) => event.stopPropagation()}><div className="modal-header"><div><p className="eyebrow">STAT PROVENANCE & CALCULATION INSPECTOR</p><h2>WHY THIS VALUE? · {breakdown.statName.toUpperCase()}</h2></div><button className="close-btn" onClick={onClose}>✕</button></div><div className="total-stat-banner"><span>FINAL TOTAL VALUE</span><b>{breakdown.totalValue}</b></div><div className="breakdown-list"><div className="breakdown-item"><span>Base Character Value</span><b>+{breakdown.baseValue}</b></div>{breakdown.permanentModifiers !== 0 && <div className="breakdown-item"><span>Permanent Modifiers / Blessings</span><b>+{breakdown.permanentModifiers}</b></div>}{breakdown.gearContributions.length > 0 ? <div className="breakdown-section"><h3>Equipped Gear Contributions</h3>{breakdown.gearContributions.map((gear, index) => <div key={index} className="breakdown-item gear-item"><span>[{gear.slot}] {gear.itemName}</span><b>+{gear.amount}</b></div>)}</div> : <div className="breakdown-item empty"><span>Equipped Gear Contributions</span><small>None</small></div>}{breakdown.activeEffectContributions.length > 0 ? <div className="breakdown-section"><h3>Active Status Effect Modifiers</h3>{breakdown.activeEffectContributions.map((effect, index) => <div key={index} className="breakdown-item effect-item"><span><i>{effect.icon}</i> {effect.effectName}</span><b>{effect.amount >= 0 ? `+${effect.amount}` : effect.amount}</b></div>)}</div> : <div className="breakdown-item empty"><span>Active Status Effect Modifiers</span><small>None</small></div>}</div><div className="modal-footer"><button className="outline" onClick={onClose}>CLOSE INSPECTOR</button></div></div></div>;
}
